namespace AlchiwebApp.PagingFiltering.Paging;

public interface IPagedList
  {
  }
  //public interface IPagedList<T, TId> where T : IEntityDto<TId>
  //{
  //    List<T> Items { get; set; }
  //    uint PageSize { get; }
  //    uint CurrentPage { get; }
  //    uint TotalPages { get; }
  //    uint TotalCount { get; }
  //    bool HasPrevious => CurrentPage > 1;
  //    bool HasNext => CurrentPage < TotalPages;
  //}
